version: '3.8'

services:
  vex:
    image: ghcr.io/counhopig/vex-bot:latest
    container_name: vex-bot
    restart: unless-stopped

    # Command to start (use --web-only for web-only mode, remove it for full mode)
    command: ["start", "--web-only"]

    # Port mapping
    ports:
      - "3000:3000"

    # Volume mounts for persistent data and optional config
    volumes:
      - vex-data:/home/vex/.vex

    # Health check
    healthcheck:
      test: ["CMD-SHELL", "node -e \"require('http').get('http://localhost:3000/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))\""]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

    # Resource limits (optional)
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 128M

# Named volume for persistent data
volumes:
  vex-data:
    driver: local
